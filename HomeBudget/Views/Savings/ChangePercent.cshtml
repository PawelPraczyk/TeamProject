@model HomeBudget.Models.Category
@{
    ViewBag.Title = "ChangePercent";
    Layout = "~/Views/Shared/Layouts/AFTERlogin.cshtml";
}
<div class="jumbotron" style="height:430px; position: relative;top:220px;background: rgba(90, 166, 130, 0.9); border: 0.5px solid black;">

<h2>ChangePercent</h2>
@using (Html.BeginForm("ChangeSavings", "Savings"))
{
    <div class="form-group" style="width:200px; margin-left:470px">
        @Html.LabelFor(m => m.User.SavingsAmount)
        @Html.TextBoxFor(m => m.User.SavingsAmount, new { @class = "form-control" })
        @Html.ValidationMessageFor(m => m.User.SavingsAmount)
    </div>

    <button type="submit" class="btn btn-primary" style="width:200px; margin-left:470px">Save</button>
}

@using (Html.BeginForm("ChangePercent", "Savings"))
{
    <div class="form-group" style="width:200px; margin-left:470px">
        @*@for (int i=0; i<Model.Categories.Count();i++)
            {
                @Html.LabelFor(m => m.Categories.ElementAt(i).Name)
                @Html.TextBoxFor(m => m.Categories.ElementAt(i).PercentMoney);
                @Html.ValidationMessageFor(m => m.Category)
            }*@

            @Html.LabelFor(m => m.category)
            @Html.DropDownListFor(m => m.category, new SelectList(Model.Categories, "Id", "Name", "Color"), "", new { @class = "form-control" })
        </div>
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group" style="width:200px; margin-left:470px">
            @Html.LabelFor(m => m.PercentMoney);
            @Html.TextBoxFor(m => m.PercentMoney);
            @Html.ValidationMessageFor(m => m.PercentMoney)
        </div>

        <button type="submit" class="btn btn-primary" style="width:200px; margin-left:470px">Save</button>

    }
    </div>